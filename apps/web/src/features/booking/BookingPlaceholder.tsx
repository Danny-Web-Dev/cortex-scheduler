import { EmptyState } from '@/components/ui';

// Replaced by the full slot-hold stepper in Phase 6.
export const BookingPlaceholder = () => (
  <EmptyState
    title="Booking flow coming next"
    description="The specialty → doctor → slot → confirm stepper lands in the next step."
  />
);
